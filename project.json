{
  "name": "sigstat",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "targets": {
    "publish-all": {
      "command": "npx nx run-many --parallel=3 --target=publish",
      "dependsOn": []
    }
  }
}
